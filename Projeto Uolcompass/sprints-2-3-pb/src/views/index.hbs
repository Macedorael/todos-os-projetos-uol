<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--google fontes-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <!--boodstrap icons-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/png" href="/assets/images/clima.png">
    <title>CLIMA Cast</title>
</head>
<body>
           
    </header>
     <header class="header">
          <div class="logo">
                <a href="#">
                    <img src="/assets/images/clima.png" alt="logo do projeto">
                </a>
            </div>

        <nav class="navbar">
            <a href="#" onclick="abrirModal()">Previsão do tempo</a>
        </nav>

    </header>
     

    <!--Expansão da janela modal-->
     </div>
    <div class="janela-modal" id="janela-modal">
         <!--div do conteudo do modal-->
        <div class="modal" id="modal">
            <button class="fechar" id="fechar">X</button>
           <form action="/search" method="GET" id="form-modal">    
                <label>
                        Digite a cidade escolhida:
                        <input type="text" id="cidade" name="cidade" required>
                    </label>
                    <label>
                        Digite o seu bairro:
                     <input type="text" id="bairro" name="bairro" required>
                </label>
                <button type="submit" id="btn-modal" class="btn-modal">Buscar</button>
            </form></br>
            
          <div class="col-auto">
          
            </div>
             <!--chamada da função que busca e devolve os valores pesquisados na API-->
           {{#if error}}
            <div class="alert alert-danger" role="alert">
      {{error}}

    {{/if}}

    {{#if previsao}}
    <section class="container-modal">
      <h2>{{bairro}}</h2>
      <p>Cidade: {{previsao.cidade}}</p>
      <p class="card-title">País: {{previsao.pais}}</p></br>
      {{#each previsao.previsoes}}
       
           
            <div class="card mb-3">
            
                <div class="card-body" >
                <h5 class="card-title">{{this.dt_txt}}</h5>
                <p class="card-text">Temperatura atual: <font style="font-weight: bold;">{{this.main.temp}} °C</font></p>
                <p class="card-text">Temperatura mínima: {{this.main.temp_min}} °C</p>
                <p class="card-text">Temperatura máxima: {{this.main.temp_max}} °C</p>
                <p class="card-text">Descrição: {{this.weather.[0].description}}</p>
                <img src="http://openweathermap.org/img/wn/{{this.weather.[0].icon}}@2x.png" alt="Ícone de previsão do tempo">
            </div> 
     </div>
    
      {{/each}}
    </section>
    {{/if}}
        </div>
</div>
 <!--conteudo e botão de pesquisar-->
<div class="previsao-container">
            <div class="interface">
                <h2 class="titulo"> Veja a previsão <span>Em qualquer lugar!</span></h2>
                <div class="flex">
                    <div class="previsao-box"  onclick="botao_pesquisar()" >
                        <i class="bi bi-file-earmark-richtext"></i>
                        <h3>Previsão do tempo</h3>
                        <div id="botao-pesquisar" onclick="abrirModal()">Clique aqui para pesquisar</div>
                    </div></br>
    </div>

    
<script src="/js/modal.js"></script>


</body>
</html>